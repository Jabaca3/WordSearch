

public class hashTable {
	int arraySize;
	bstNode[] bstArray;
	bstNode bst;
	/* Constructor */
	
	public hashTable(int size) {
		arraySize = size;
		bstArray = new bstNode[size];
		

	}
}
